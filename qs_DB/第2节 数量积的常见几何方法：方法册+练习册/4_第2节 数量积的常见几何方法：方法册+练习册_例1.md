【例1】 边长为1的正方形ABCD 中， $E$ ， $F$ 在线段 $B D$ 上，且 $\left| { \overrightarrow { D E } } \right| = \left| { \overrightarrow { B F } } \right|$ ，则：


（1）

若 $\left| { \overrightarrow { B D } } \right| = 4 \left| { \overrightarrow { D E } } \right|$ ，则 ${ \overrightarrow { A E } } \cdot { \overrightarrow { A F } } = .$ [] _；（2） ${ \overrightarrow { A E } } { \cdot } { \overrightarrow { A F } }$ 的取值范围是
